# Inference (torch >= 2.9 with CUDA must be installed first, matching your GPU/driver)
ms-swift==4.3.2
transformers==5.12.1
peft==0.19.1
accelerate==1.12.0
qwen-vl-utils==0.0.14
av==15.1.0
imageio-ffmpeg==0.6.0
huggingface_hub>=1.0
pyarrow>=16
# Evaluation with the official metric
orena-focus==0.3.5
